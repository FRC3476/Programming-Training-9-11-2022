import java.util.Scanner;

/**
 * An abstract animal class that Dog, Cat, and GoldenRetriever classes extend from.
 */
public abstract class Animal {

    // Fields, contains name and birthday

    // Create a scanner object from the java,util,Scanner class, which we will use for detecting text input

    // Constructor, Will take in a name, and birthday

    // Methods

    // Detect input wrapper method (uses scanner class)

    // Create default play method
}
